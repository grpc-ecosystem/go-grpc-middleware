---
version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet

  settings:
    errcheck:
      exclude-functions:
        - (github.com/go-kit/log.Logger).Log

    govet:
      disable:
        - fieldalignment
      enable-all: true

  exclusions:
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling

formatters:
  enable:
    - gofumpt
    - goimports

  settings:
    gofumpt:
      extra-rules: true
